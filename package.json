{
  "name": "@macawkit/base",
  "version": "0.2.0",
  "description": "Base classes for MacawKit projects",
  "main": "dist/index.js",
  "module": "dist/index.mjs",
  "browser": "dist/index.mjs",
  "types": "dist/index.d.ts",
  "files": [
    "dist",
    "src"
  ],
  "scripts": {
    "clean": "rm -rf dist",
    "prebuild": "npm run clean && tsc",
    "build": "node build.mjs",
    "build:tests": "npm run build test",
    "build:release": "npm run build release",
    "pretest": "npm run build:tests",
    "test": "node --test --experimental-test-coverage",
    "prepack": "npm run build:release"
  },
  "author": {
    "name": "Yury Gubich",
    "email": "blue@macaw.me"
  },
  "repository": {
    "url": "git+https://github.com/macawkit/base",
    "type": "git"
  },
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^20.14.11",
    "esbuild": "^0.23.0",
    "typescript": "^5.5.3"
  }
}
